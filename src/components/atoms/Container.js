const Container = (props) => {
    return (
        <section className="w-full mx-auto max-w-7xl mx-auto px-4 py-4">
            { props.children }
        </section>
    )
}

export default Container